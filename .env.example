# Root .env.example — consumed by docker-compose.yml (Postgres service).
# Copy to .env and fill real values for local dev. Never commit a real .env.
# Variables consumed here MUST also exist in server/.env.example and ai/.env.example (R10).

DB_USERNAME=postgres
DB_PASSWORD=change_me
DB_NAME=mma_db
DB_PORT=5432